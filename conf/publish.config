process {  
  withLabel: download {
    publishDir {
      path = "${params.outdir}/download"
      mode = "link"
    }
  }
  withName: collateResults {
    publishDir {
      path = "${params.outdir}/stats"
      mode = "${params.publishmode}"
    }
  }
}
